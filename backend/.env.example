# backend/.env.example
PORT=3000
NODE_ENV=development

# Database Configuration (PostgreSQL)
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=casalivraison
DB_HOST=localhost # Set to 'db' for Docker, 'localhost' for local development
DB_PORT=5432

# JWT Secret
JWT_SECRET=your_jwt_secret_key_here
