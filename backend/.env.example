# backend/.env.example
PORT=3000
NODE_ENV=development

# Database Configuration (PostgreSQL)
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=casalivraison
DB_HOST=db
# Pour Docker: db (service name in docker-compose)
# Pour Local: localhost
DB_PORT=5432
